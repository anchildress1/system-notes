# lefthook.yml
pre-commit:
  parallel: false
  commands:
    format:
      run: npm run format
    # lint:
    #   run: npm run lint   # Disabled to unblock commit
    test:
      run: npm run test

commit-msg:
  commands:
    commitlint:
      run: npx --no-install commitlint --edit
